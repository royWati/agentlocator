package com.chainbox.ussd.agentlocator.DatabaseRepository;

import com.chainbox.ussd.agentlocator.Entities.tb_thread;
import org.springframework.data.repository.CrudRepository;

public interface thread_repo extends CrudRepository<tb_thread,Integer> {
}
